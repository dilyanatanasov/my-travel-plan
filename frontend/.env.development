# Dev counterpart to .env.production. Without it Vite warns on every start
# that %VITE_PUBLIC_URL% is undefined, and the og tags in the served HTML
# carry the raw placeholder.
VITE_PUBLIC_URL=http://localhost:5173
