# Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=travel_tracker

# Backend
NODE_ENV=development
DB_HOST=db
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_DATABASE=travel_tracker
API_PORT=3000

# Frontend
VITE_API_URL=http://localhost:3000/api

# Flight Search API (Kiwi.com via RapidAPI)
# Get your key at: https://rapidapi.com/Developer-API/api/kiwi-com-cheap-flights
# Single key (legacy):
RAPIDAPI_KEY=
# Multiple keys (comma-separated) for rotation when quota is exceeded:
RAPIDAPI_KEYS=key1,key2,key3

# Auth
# Generate with: openssl rand -base64 48
JWT_SECRET=
JWT_EXPIRES_IN=7d
# Comma-separated allowlist of frontend origins
CORS_ORIGIN=http://localhost:5173

# Production domains (unused in dev — the dev stack has no nginx)
# Canonical domain the app is served on. nginx and CORS both key off this.
DOMAIN=
# Optional second domain that 301-redirects to DOMAIN (e.g. an .app twin).
# Leave empty until setup-ssl.sh has issued its certificate; the frontend
# container falls back to single-domain HTTPS while this is unset.
ALT_DOMAIN=

# Transactional email (password reset + verification). Unset = mail is
# logged by the backend instead of sent — dev and CI send nothing.
RESEND_API_KEY=
MAIL_FROM="myContrail <no-reply@mycontrail.com>"

# Web push (trip anniversaries). Unset = the toggle reports push as
# unavailable and the backend logs instead of sending — dev and CI push
# nothing. Generate a pair once with: npx web-push generate-vapid-keys
# The public half is served to browsers via GET /api/push/public-key.
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:no-reply@mycontrail.com

# Umami analytics (prod only). Random secret for its session signing:
#   openssl rand -base64 32
UMAMI_APP_SECRET=
# Dedicated umami DB role (hex — URL-safe inside DATABASE_URL):
#   openssl rand -hex 24
UMAMI_DB_PASSWORD=
